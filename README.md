# tsdeftool

Truck Simulator definition tool (American &amp; Euro 2)

## Usage

Run app in directory, where your source files (*.sui) are. It'll recursively pick up all the files and generate the
definitional files (*.sii).

You can also run it from the command line for additional options.
